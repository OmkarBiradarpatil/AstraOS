# AstraOS Shared UI

Reserved for shared React UI primitives after the preserved UI has visual parity coverage.

Do not extract UI from `apps/web/index.html` until each component has screenshot parity tests.

